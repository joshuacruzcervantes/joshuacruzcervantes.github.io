/**
 * PostCSS runs Tailwind and Autoprefixer when Next.js compiles the CSS.
 * You normally never need to touch this file.
 */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
